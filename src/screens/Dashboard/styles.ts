import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#6B4284',
    padding: 20,
    paddingBottom: 0,
    gap: 8,
  },
  content:{
    marginBottom: 20
  },
  scroll: {
    gap: 8,
    paddingBottom: 32
  }
});